# ACCV 2026 投稿脱敏机械替换表（prep 工件，非稿）

> **用途**：为 ACCV 2026（双盲）定稿前一次性脱敏扫做准备。本文件是 prep 清单，**不是论文稿**。
> **服务规则**：STORY R4（方法名脱敏）+ R10（BMVC 只 cite 不搬）+ ACCEPTANCE 红线 10（匿名违规：投稿前 grep 全套内部品牌名 + 旧占位串必须 0 命中，名单见下方扫描清单）。
> **建表时间**：2026-06-19（会话内 grep 实证，未改任何 .tex）。
> **扫描范围**：ACCV 投稿真源 = `main_accv.tex` + `preamble_accv.tex` + `drafts_accv/*.tex` + `figures/fig1_pipeline.tex` + main_accv `\input` 的 `appendix/A*.tex`（A0/A1/A2/A2_3/A3/A4/A5/A5_2/A18/A19/A20/A21/A23/A26/A27/A28）+ `references.bib`。

---

## ⭐ 核心结论（先看这条）

**ACCV 稿的脱敏机制已基本就位，不是"满稿真名待替换"的局面。** 关键事实：

1. **方法名全部走 `\newcommand` 宏**（`preamble_accv.tex` L37-42），且**宏的 RHS（右值）已经是匿名通用名**。正文/附录/图引用的是宏（`\visienhance` 等），渲染出来已经是 `QP-Enhance` 等匿名名 —— **不存在渲染层真名泄露**。
2. **author / institute / email 已占位** 为 `Anonymous Authors` / `Anonymous Institution` / `anonymous@example.com`（`main_accv.tex` L79-83）。
3. **figure（fig1_pipeline.tex）已用通用名**（"5-head IQA Assessor" / "DP-Enhance" / "Query-for-Retake"），无真名硬编码。
4. **BMVC 自引已规范**为 `@misc{ANON-BMVC}`（references.bib L494-499），title/author/note 全脱敏。
5. **`atcxr2025` 不是 TODO bib** —— 它是真实外部文献 AT-CXR（arXiv:2508.19322，references.bib L486-492），条目完整。〔订正主线背景："atcxr2025 是 TODO bib" 系误记。〕

**因此真正待脱敏的工作量很小**：核心是定稿时 de-anon（把宏 RHS 改回真名 + 填真实 author/institute）的**反向操作**——即"现在已匿名，camera-ready 才反填"。投稿前需做的只是：(a) 确认无人绕过宏写硬编码真名；(b) 注释清理（可选，注释不渲染但保险起见）；(c) 占位符 grep 全 0 验证。

---

## 📋 替换表

> 注：「建议替换名」一列对**已经匿名的项**标注「✅ 已匿名（保持）」；只有真正需要动的才给替换动作。

| 原名 / 项 | 出现文件:行（grep 实证）| 当前状态 | 建议处置 | 类别 | 注意事项 |
|---|---|---|---|---|---|
| `\visienhance` → `QP-Enhance` | 宏定义 `preamble_accv.tex:40`；引用散见 `s1_intro.tex:30,45,52`、`s4_c1_enhance.tex:20,74`、`s7_experiments.tex:87,124,129,135`、`s8_discussion.tex:31`、`s5_c2_agent.tex:28`、`main_accv.tex:98,106,131`、`appendix/A28:33`、`appendix/A2_3_theorem2`、`s5_c2_agent` | ✅ 宏 RHS 已匿名 = `QP-Enhance` | ✅ 已匿名（保持）。**一改全改**：camera-ready 只改 `preamble_accv.tex:40` 右值即可全文翻真名。**不需投稿前动。** | 方法（系统宏）| 真名 = 内部品牌「VisiEnhance」（及其 `-Net` 后缀变体）。**这是本文贡献"诊断保持增强器"，camera-ready 保留为真名，非删除项。** |
| `\visiscore` → `5-head IQA` | 宏定义 `preamble_accv.tex:39`；引用 `s1_intro.tex:29,33`、`s5_c2_agent.tex:28`、`appendix/A0:15`、`A1_qvib:12`、`A18:13`、`A4_itb:17`、`A2_3_theorem2:8` | ✅ 宏 RHS 已匿名 = `5-head IQA` | ✅ 已匿名（保持）。一改全改同上。 | 方法（系统宏）| 真名 = 内部品牌「VisiScore」（及其 `-Net` 后缀变体）。fig1 文字「5-head IQA Assessor」与宏 RHS 一致，camera-ready 时图与文需同步改。 |
| `\qvib` → `QC-VIB` | 宏定义 `preamble_accv.tex:38`；引用密集于 `appendix/A1_qvib`（L18,29,31,110,279,331）、`A1_qvib_compact`、`A2_prop3_lemma3`（L17,71,90,237,240）、`A3_corollary1`（L8,10,80）、`A20`（L132,141,198）、`A21:100` | ✅ 宏 RHS 已匿名 = `QC-VIB` | ✅ 已匿名（保持）。一改全改同上。 | 方法（系统宏）| 真名 = 内部品牌「QVIB」（连字符形式，即 quality-conditioned VIB）。**STORY 已降级为脚注/框架**（不 claim 增益），但名字本身仍是要保留的命名，camera-ready 反填真名。 |
| `\qcts` → `QC-TS` | 宏定义 `preamble_accv.tex:41`；引用 `appendix/A3_corollary1_compact:4` 等 | ✅ 宏 RHS 已匿名 = `QC-TS` | ✅ 已匿名（保持）。 | 方法（系统宏）| 真名 = QCTS。camera-ready 反填。 |
| `\itb` → `QSB` | 宏定义 `preamble_accv.tex:42`；引用 `appendix/A4_itb_construction`、`A18` 等 | ✅ 宏 RHS 已匿名 = `QSB` | ✅ 已匿名（保持）。 | 方法（自建 benchmark 名）| 真名 = ITB（自建退化 benchmark）。注意 ITB 这个内部缩写若出现在**注释或非宏文本**需单独清（见下"注释"行）。 |
| `DP-Loss` / `DP-Enhance` / `query-for-retake` | `s1_intro.tex:45`、`fig1_pipeline.tex:7,157`、全篇 | 通用描述性名（非身份名）| ✅ **保留，不脱敏**。 | 贡献新名 | ⚠️ **这些是本文新命名的贡献本身**（诊断保持损失 / 增强器 / 采集闭环钩子），**不是会暴露身份的旧专名**。**严禁误列进删除。** query-for-retake 是 C2 独家钩子（STORY 跑偏定义 #8）。 |
| author / authorrunning | `main_accv.tex:79-80` | ✅ 已占位 `Anonymous Authors` / `Anonymous et al.` | ✅ 已匿名（保持）。camera-ready 填真名。 | 人名 | TODO FINAL 注释已标。投稿前确认仍是 Anonymous。 |
| institute / email | `main_accv.tex:83` | ✅ 已占位 `Anonymous Institution` / `anonymous@example.com` | ✅ 已匿名（保持）。 | 机构 | 真实机构 = XJTLU / 西交利物浦。投稿前确认未反填。 |
| 小 LM 名（Qwen3-4B）| `s5_c2_agent.tex:90`（TODO 注释，**渲染层用泛称**）| ⚠️ 正文用泛称「small-LM / ReAct」，真名 Qwen3-4B 仅在 TODO 注释 | ✅ 正文已泛化（保持）。**注释里的 Qwen3-4B 建议投稿前删/改注释**（保险，注释不渲染但避免源码包泄露）。 | LM | STORY C2 记小 LM = Qwen3-4B。**确认正文 §5.3 渲染文本是否含真名"Qwen3-4B"** —— grep 显示仅注释含，正文用 "ReAct + rule fallback" 泛称。✅ 渲染安全；注释清理可选。 |
| `@misc{ANON-BMVC}` 自引 | `references.bib:494-499`；引用 `s6_setup.tex:40`、`s7_experiments.tex:40` | ✅ 已脱敏（title=Anonymized prior work / author=Anonymous / note 说明 withheld）| ✅ 已匿名（保持）。 | bib（自引）| R10：BMVC 只 cite 不搬数。camera-ready 反填真实 BMVC 引用（且需 BMVC notification ~2026-08 公开后）。 |
| `@article{atcxr2025}` | `references.bib:486-492`；引用 `s5_c2_agent.tex:22` | ✅ 真实外部文献（AT-CXR, arXiv:2508.19322），非自引 | ✅ **保留，不脱敏**（外部 cite 不涉身份）。 | bib（外部）| 〔订正："atcxr2025 是 TODO bib" 系误记，条目完整无 TODO。〕 |
| `\includegraphics{figures/fig1_pipeline}` 编译产物 PDF | `main_accv.tex:127` | fig1_pipeline.tex 源已用通用名 | ⚠️ **重新编译 fig1_pipeline.pdf 后再验**（grep 看 .tex 源安全，但若仓库里存的是旧 PDF，PDF 内嵌文字 grep 不到）| 图（PDF 产物）| **注意事项**：脱敏扫时 PDF 是二进制，grep 真名扫不到。投稿前用最新 fig1_pipeline.tex 重编 PDF，肉眼核图中文字无真名。 |

---

## 🔢 脱敏执行顺序建议

> 现状是"已匿名"，所以投稿前是**验证 + 收尾**，不是大规模替换。顺序如下：

1. **先验宏定义层（5 个宏，一处定全局）**：确认 `preamble_accv.tex:38-42` 五个 `\newcommand` 的 RHS 全是匿名名（`QC-VIB`/`5-head IQA`/`QP-Enhance`/`QC-TS`/`QSB`）。这 5 行是**唯一的总开关** —— 只要 RHS 匿名，所有 `\visienhance` 等宏引用全渲染匿名，无需逐处改正文。✅ 当前已满足。

2. **再扫硬编码真名（绕过宏的漏网）**：对 `main_accv.tex` + `drafts_accv/*.tex` + `appendix/A*.tex` + `figures/fig1_pipeline.tex` 跑 grep（见下方扫描清单），确认无人直接写字面真名 `VisiSkin` / `VisiScore` / `VisiEnhance` / `QVIB`（连字符形式，不带反斜杠宏）。✅ 当前 grep 0 命中渲染层真名。

3. **清注释里的真名/内部缩写（可选但建议）**：注释（`%` 开头）不渲染进 PDF，但若投稿要交 LaTeX 源码包，保险起见清掉注释中的 `Qwen3-4B`（`s5_c2_agent.tex:90`）、内部 benchmark 缩写 ITB（若散在注释）等。**这是逐个手动，不是宏一改全改。**

4. **验 author/institute/email 三处占位**（`main_accv.tex:79-83`）仍为 Anonymous，未提前反填真实机构。

5. **重编 figure PDF 并肉眼核**：用最新 `fig1_pipeline.tex` 重编 `fig1_pipeline.pdf`，确认图中文字无真名（PDF grep 扫不到，必须肉眼）。

6. **最终 grep 全 0 验证**（ACCEPTANCE 红线 10）：对全 ACCV 稿（含 .tex 源 + bib）grep 下列名单必须 0 命中：
   - `VisiSkin` / `VisiScore` / `VisiEnhance`（字面真名，不含 `\visi...` 宏）
   - `QVIB`（连字符与无连字符两种写法的字面真名，不含 `\qvib` 宏）
   - `anonymous20XX`（旧占位风格残留，含历史年份后缀的所有变体）
   - `Qwen` / `Qwen3-4B`（渲染层）
   - `XJTLU` / `Xi'an` / `Jiaotong` / `Liverpool` / `西交利物浦` / 真实人名 / `github.com/<真实账号>` / grant 号

---

## 🟢 保留清单（贡献新名，严禁误删）

下列是**本文贡献本身的命名 / 描述性术语**，是论文卖点，**不在脱敏范围**：

- **DP-Loss**（diagnosis-preserving loss，C1 核心）
- **DP-Enhance**（fig1 中对增强通道的描述名；与匿名宏 `QP-Enhance` 同指本文增强器）
- **query-for-retake**（C2 独家采集闭环钩子，STORY 跑偏定义 #8 钉死不可简化为 abstain/defer）
- **5-head IQA / per-input quality scalar**（质量评估的通用描述）
- 四通道名：direct diagnose / cautioned diagnose / enhance-then-diagnose / query-for-retake
- 外部真实文献 cite：AT-CXR (`atcxr2025`)、NAFNet、Restormer、SwinIR 等 baseline 名

> ⚠️ 区分逻辑：**会暴露作者身份的内部专名**（VisiSkin / VisiScore / VisiEnhance / QVIB（连字符形式）/ QCTS / ITB —— 这些是内部品牌名）= 脱敏；**领域通用术语 + 本文新方法的描述性命名**（DP-Loss / query-for-retake）= 保留。前者已全部走匿名宏，后者直接明文写。

---

## ⚠️ 共享附录宏注意

`appendix/A*.tex` 与 ICLR 版**共享同一批宏**（A1_qvib / A2_prop3_lemma3 / A3_corollary1 / A4_itb / A18 / A20 等全用 `\qvib`/`\visiscore`/`\visienhance`/`\itb`/`\qcts`）。这意味着：

- camera-ready 反填真名时，**只改 `preamble_accv.tex` 的 5 个宏 RHS**，ICLR 版与 ACCV 版的附录都会跟着翻（如果它们共享同一 preamble；ACCV 用 `preamble_accv.tex`，ICLR 用 `preamble.tex` —— **需确认两个 preamble 的宏 RHS 独立维护**，避免改一处漏一处）。
- ⚠️ **建议**：定稿时核对 `preamble.tex`（ICLR）与 `preamble_accv.tex`（ACCV）两份的宏 RHS 一致性，防止一份匿名一份真名的不一致泄露。

---

## 📌 留给 verifier / 主线的 TODO 占位

- `\todo{核 verifier}`：无新数字引入，本文档不涉数字核算。
- 小 LM 名 Qwen3-4B：STORY 记为 Qwen3-4B，**正文渲染已用泛称**，注释含真名待定稿前清（见执行顺序 #3）。
- BMVC 自引反填：需等 BMVC notification ~2026-08 公开后才能填真实引用（R10）。
