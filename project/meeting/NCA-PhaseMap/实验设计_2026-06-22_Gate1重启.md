# NCA-PhaseMap Gate1-重启 · 4 补实验矩阵（planner，2026-06-22）

> 服务降级叙事「update 稀疏度功能塌缩**条件性**刻画 + 机制 + 安全 fire_rate 区间」。复现零偏离（超参全沿用 B1_B2_B3_sweep.py 官方值 LR16e-4/betas(0.5,0.5)/300step/ch16/no-clip 主条件）。

## 关键既有事实（已核 csv，复用不重跑）
- collapse 阈冻结（B0_baseline.csv）：BraTS **0.012883** / Hippo **0.116746**。补实验全从此读阈，不重标。
- B3 已覆盖 ur∈{0.625,0.65,0.675,0.70,0.725}×5seed no-clip（塌缩率 2/1/2/3/3 全 MIXED）。
- M1 bug 根因实锤：`fc1.weight.zero_()` + 单步 forward → 响应恒零（非 theta 问题，修法=换探针物理量）。

## 补1【生死前置】BraTS 扩 ur 打 A2 —— 先跑，定其余做不做
- **E1**：update_rate ∈ {0.45,0.50,0.55,0.60,0.75,0.80}（补 B3 两端；或直接全 0.45–0.80 步0.05 省改码）× 5 seed，no-clip/官方超参/B0 阈/BraTS test。
- **判据（预登记，continuous=collapse_rate vs ur 曲线）**：
  - 🟢 翻案 PASS：存在连续区间 collapse_rate 单调跨 0→1（低端≥1 档 0/5 + 高端≥1 档 5/5 + 过渡≤0.10ur）→「数据集依赖确定临界」，降级叙事升格"条件性相变成立"。
  - 🔴 **KILL（触 K-new-1）**：全 0.45–0.80 无任何档 5/5 且无 0/5（全 MIXED）→ BraTS 无确定相变=seed 随机，**彻底收口不再重启**。
  - 灰区（停报）：高端 5/5 但过渡 >0.10 → headline 改"区间一致点位数据依赖"。
- 复用 `B1_B2_B3_sweep.py --stage B3`（coder 改 build_grid_b3 支持 8 档 ur 列表）。算力 **~0.3 GPU·h**（40 run）。

## 补2 修 M1 probe（机制段，待补1 PASS）
- bug 修法：路径A（推荐）=多步传播(steps≥16)+训练后/非零权重；路径B=mask 连通半径。
- E2b：update_rate 9 档 × 3 seed × Hippo+BraTS，测传播半径 d(ur) vs collapse_rate(ur) 关联。
- 判据：🟢 d(ur) 临界处拐点且与塌缩方向一致(Spearman 显著) / 🔴(触 K-new-3) 单调无拐点→机制段降级。
- **🔴 TODO 派 researcher**：机制探针物理量选型（脉冲传播半径 vs update 算子 Jacobian 谱半径，哪个是 NCA 稳定性标准量；arXiv 2310.14809 出处核实；theta 标准）。**地基盲区，未调研不定稿。**
- ⚠️ skeptic 须攻：路径A「用训练后权重测机制」可能与「机制独立于训练」循环论证。
- 算力 <0.05 GPU·h（纯前向，可本地烟测）。

## 补3 Hippo vs BraTS 差异机制（待补1 PASS）
- E3a 前景占比对照（BraTS 筛 fg 匹配 Hippo）+ E3b 强度/归一对照 + E3c Hippo 反向退化(可选)。
- 判据：🟢 某单变量翻转使 collapse_rate 变确定→找到"相变成立条件" / ⚪ 都不变→差异不可归因，弱化。
- **TODO 核**：Hippo 官方归一化 vs data_brats.py min-max 是否一致（E3b 对齐前提）。
- 算力 ~0.1 GPU·h（10–15 run）。

## 补4 对齐第二实现（待补1 PASS）
- E4a 诊断 MinimalNCA vs 官方 BackboneNCA 逐层 diff（为何 dice 0.3 vs 0.7）→ E4b 对齐后跨实现验 Hippo ur* 断崖。
- 判据：🟢 对齐后 dice~0.70 且复现断崖→跨实现 PASS / 🔴 修不对等→诚实标"无有效跨实现对照"(非主 KILL)。
- **TODO 派 researcher**：官方 BackboneNCA 完整架构（感知核 identity+laplacian？双层级联？）+ 第三方独立 NCA 实现（避自造再不对等，禁照搬别库超参）。
- 算力 ~0.15 GPU·h（18 run）。

## 依赖图
```
补1 E1 (生死闸门, 40run, 先跑独占决策)
  └ analyst 判 K-new-1
      ├ 🔴 全MIXED → 彻底收口, 补2/3/4 不做 (省 0.25 GPU·h)
      └ 🟢 出现 5/5或0/5 → 补2/3/4 三路并行(无文件冲突, HPC 4卡各1)
```
全做 ~0.6 GPU·h；补1 触 K-new-1 收口则 ~0.3 GPU·h。NCA 万级参数，HPC 单卡墙钟 <1h。

## 前置 TODO（交 coder 前须解）
1. 🔴 机制探针物理量选型（补2，派 researcher）—— 地基盲区。
2. Hippo 归一化对齐核（补3 E3b）。
3. 官方 BackboneNCA 架构 + 第三方对等实现（补4，派 researcher）。
4. collapse 阈从 B0 读不重跑/不改（改阈=拍板点）。

## 下一棒
skeptic 红队本设计（攻补1 K-new-1 二元判据 + 补2 训练后权重循环论证，0 致命即过）→ coder 实现补1（小改 build_grid_b3）→ 主线 gpu_slot 跑补1 → analyst 判 K-new-1 → 据结果决定补2/3/4。
