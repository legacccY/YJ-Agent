> DRAFT 2026-07-04 待余嘉审后呈袁老师；问题③④为原《给袁老师_两个方法学问题》两问原样保留。

# QuantImmuBench 投稿前方法学口径决策档

袁老师您好。这份是投稿 Briefings in Bioinformatics 前需要您拍板的 **5 个方法学口径**。前 2 个（问题①②）是本轮新查清、之前没定的重点，后 3 个（问题③④⑤）是之前已经提过、这里一并汇总请您定。每个问题都按「事实（已 Bash 核实）→ 选项 / 影响 → 我方推荐 + 理由 → 想问老师」来写，数字都标了来源，推荐只是建议、决定权在您。

真源文件：`data/frozen/pooled_clean_9mer.csv`、`pooled_clean_8to11mer.csv`、`scripts/out/merged_all_tools_30_official_covfix.csv`、`WT_NA_indel_list.csv`、`data/.../ELISPOT_OFFICIAL_Braun2025_MOESM4.xlsx`。

---

## 问题① — 8-11mer 补充分析的 indel 口径（核心新增，最需拍板）

### 事实（已 Bash 核实）

- **130 肽 = 101 SNV + 29 非 SNV**。官方 In Vitro 页 `Variant_Type` 字段逐条核：SNV 101 / DEL 23 / INS 5 / None 1。
- **子肽展开口径对两类变异不对称**（这是关键，也是之前旧 04_LOG 记录不精确的地方）：
  - 取代型（SNV / 错义）肽做了完整 8-14mer 全展开——SNV 肽 **100/101 全展开**。
  - 真 indel（DEL 23 + INS 5 = **28 肽**）只做了 9mer 窗展开。
  - 逐 indel 肽核：29 个 indel_list 肽里 **28 个仅 9mer 子肽**，只有 1 个例外（`16097-104-24`）有完整 8-14mer 展开——而这 1 个例外本质是 `AMACR|p.Y41N` **错义点突变**（`Variant_Type=NaN`，只因 WT=NA 才落进 indel_list），并非真 indel。
- **这不是工具没打分、也不是 bug**，而是 preprocessing 阶段子肽展开对两类变异用了不同窗口。工具对已生成的非 9mer indel 子肽（200 行，全来自那 1 例外肽）该打分的都打了：21 工具 100% 覆盖、5 工具部分覆盖、4 工具 0%（其中 DeepNetBim / NeoaPred 是架构级 9mer-only）。
- **路 B 相关的工具数**：30 个打分列里，**28 个在 SNV 上有真·8-11mer 窗变化**，只有 2 个是架构级 9mer-only（**DeepNetBim、NeoaPred**）。

一句话：8-11mer 这条补充分析，本来就只有 SNV 肽有一致的多长度展开；indel 在主分析和补充分析里都只贡献 9mer 池化。

### 三个选项 / 影响面

| 选项 | 做什么 | 影响面 | 成本 |
|---|---|---|---|
| **路 A** | 重跑补 28 个 DEL/INS 肽的 8/10/11mer 子肽窗 + 重打分 | 仅补充材料 8-11mer 分析、仅 28 肽；主 9mer 分析完全不受影响 | 覆盖战役级 HPC 重跑（机时 [待核]） |
| **路 B（推荐）** | 8-11mer 补充分析限定 **101 SNV 肽 × 28 支持窗变化的工具**，剔除 indel | 补充分析口径干净；主分析不变 | 零重跑 |
| **现状** | 保留全集，§2.2 加脚注说明口径不对称 | 无重跑，但需诚实披露不对称 | 零重跑 |

### 我方推荐 + 理由

**推荐路 B**。理由：8-11mer 分析的立意是「多长度窗口敏感性」，而只有 SNV 肽真正做了多长度全展开，indel 本就只有 9mer——限定 SNV 子集是 apples-to-apples，不是回避问题，而是让这条补充分析对齐它真正测的东西。零重跑，且披露成本最低。路 A 的产出仅覆盖 28 肽的补充分析、对主结论无影响，投入产出比低；现状可接受但需要一段脚注解释不对称，读者理解成本略高。

### 想问老师

8-11mer 补充分析是否按**路 B（限定 101 SNV 肽）**定稿？还是您认为应走路 A 补齐 indel、或保留现状加脚注？

### §2.2 措辞三版草案（英文，投稿用）

**现状版（保留全集 + 脚注）**
> Multi-length (8–11mer) subpeptide expansion was applied to substitution neoantigens (101 peptides); frameshift/indel neoantigens (28 peptides) were expanded at the 9-length window only. The 8–11mer supplementary analysis therefore reflects length-window sensitivity on substitution peptides, while indel neoantigens contribute at the 9mer window in both the primary and supplementary analyses.

**路 B 版（限定 SNV 子集，推荐）**
> Because a consistent multi-length (8–14mer) subpeptide expansion was available only for substitution neoantigens, the 8–11mer length-window sensitivity analysis was restricted to the 101 substitution peptides scored by the 28 tools whose pooled scores are length-window dependent (two tools, DeepNetBim and NeoaPred, are architecturally 9mer-only and excluded). Indel neoantigens are analysed at the 9mer window throughout.

**路 A 版（重跑补齐后）**
> Multi-length (8–11mer) subpeptide expansion was applied to all 130 neoantigens, including a supplementary re-expansion of the 28 frameshift/indel peptides that were initially expanded at the 9-length window only, so that the 8–11mer analysis covers both substitution and indel neoantigens.

---

## 问题② — 投稿 scope（DS2-only 确认 + 新发现）

### 事实（已核实）

- outline 声称「人鼠四数据集（人 ds1/ds2 + 鼠 B16F10/CT26）」，但**只有 DS2（130 肽 / 9 患者）跑了干净的 30 工具管线**。
- **DS1**（人，82 肽 / 6 患者，患者 ID 1–6）仅在 R9 supplementary 管线出现，不是主池化。已 Bash 核：DS1（6 患者 ID 1–6）与 DS2（9 患者 ID 101–110）**患者 ID 完全不重叠、是两个独立人类队列**（不同研究），非同批样本。DS1 若过一遍干净 30 工具管线，可作为独立人类外部验证队列。
- **鼠 B16F10 / CT26 数据全项目文件名零命中 = 数据实缺**，不在手。
- **新发现**：权威 raw `ELISPOT_OFFICIAL_Braun2025_MOESM4.xlsx` 有两个 sheet：
  - `In Vitro`（130 肽，即现用 DS2）
  - **`Ex Vivo`（36 行，疫苗池级逐周 Week0-24 时间序列，从未被用过）**
  - 两页是**同一批人类患者的两种读出**，Ex Vivo 不是鼠数据。Ex Vivo 是池级（pool-level）而非肽级，不能直接当第二个肽级评测集；但 In Vitro 页有 `Pool` 列可以挂钩，是一条潜在的正交稳健性验证轴。

### 选项 / 影响

- 投稿按 **DS2-only** 定稿：正文不写「四数据集」，DS1 与鼠数据列为 future work。scope 诚实、与手上真跑过的管线一致。
- Ex Vivo 第二读出：作为正交验证轴开发（池级 vs 肽级挂钩），或列为 future work / 可选补充。

### 我方推荐 + 理由

**推荐 DS2-only 定稿 + Ex Vivo 列 future work / 可选补充**。理由：只有 DS2 有干净的 30 工具全管线，正文写「四数据集」与实际不符、投稿有 over-claim 风险；DS1 与鼠数据据实列 future work 更稳。Ex Vivo 是同批患者的第二种读出，理论上能做正交稳健性检验，但它是池级、需要额外开发挂钩逻辑，短期不阻塞投稿，适合作为可选补充或 future work。

### 想问老师

投稿是否按 **DS2-only** 定稿（tex 不写「四数据集」，DS1 / 鼠数据列 future work）？Ex Vivo 这条第二读出要不要现在就开发成正交验证轴，还是列 future work？

---

## 问题③ — 肽长控制（保留原《给袁老师_两个方法学问题》「问题一」原文）

评价工具预测能力时发现：长肽段上区分不出能力，肽段越长、工具的免疫原性打分越高。长肽会取更多窗口，能力弱的工具会在随机概率上更容易撞上高免疫原性的片段，所以指标和真实能力会有很大不符。

简单说就是"抽签多的赢"，推导如下。一个突变有 $n$ 个窗口，工具对每个窗口打分 $X_1,\dots,X_n$（独立同分布，分布函数 $F$），池化取 $S=\max(X_1,\dots,X_n)$。

最大值的分布函数：

$$P(S\le t)=P(X_1\le t,\dots,X_n\le t)=F(t)^n$$

于是期望：

$$\mathbb{E}[S]=\int t\,\mathrm{d}\,F(t)^n=\int t\,n\,F(t)^{n-1}f(t)\,\mathrm{d}t$$

以最简单的均匀分布 $X\sim U(0,1)$（$F(t)=t,\ f(t)=1$）为例：

$$\mathbb{E}[S]=\int_0^1 t\cdot n\,t^{n-1}\,\mathrm{d}t=n\int_0^1 t^n\,\mathrm{d}t=\frac{n}{n+1}$$

$\frac{n}{n+1}$ 随 $n$ 单调递增（$n=2$ 是 0.67，$n=10$ 是 0.91，$n=50$ 是 0.98）。而且对任意分布，$n+1$ 个数的最大值一定 $\ge$ $n$ 个数的最大值，所以 $\mathbb{E}[\max]$ 恒随 $n$ 递增。**结论：哪怕工具打分是纯噪声，窗口越多、max 越高，与真实免疫原性无关。**

我们验证了：控制掉肽长之后，HLAthena 这类工具从 0.63 塌到 0.20（说明它大半靠蹭长度），而 netMHCpan_BA 从 0.39 稳到 0.42（真本事）。

**想问老师**：做工具排名时，要不要把肽长控制掉？还是认为"长肽更免疫原"是该保留的真信号？

---

## 问题④ — geomean 近亲（保留原《给袁老师_两个方法学问题》「问题二」原文）

干净数据下 geomean、mean-rank、median 的结果几乎分不开，"geomean 唯一最优"很难说。推了一下发现这是数学上必然的，不是我们算错。

**关键前提**：最后指标是 Spearman，只看排序、不看分值绝对大小。所以两个融合方法只要对肽产生相同的排序，Spearman 就相同。

设肽 $i$ 在 $D$ 个工具上的排名是 $(r_{i1},\dots,r_{iD})$：

- mean-rank 分数：$A_i=\dfrac{1}{D}\sum_{d=1}^{D} r_{id}$（算术平均）
- geomean 分数：$\log G_i=\dfrac{1}{D}\sum_{d=1}^{D}\log r_{id}$（几何平均 = log 的算术平均）

把 $\log r_{id}$ 在肽内均值 $A_i$ 处做二阶泰勒展开：

$$\log r_{id}\approx \log A_i+\frac{r_{id}-A_i}{A_i}-\frac{(r_{id}-A_i)^2}{2A_i^2}$$

对 $d$ 求平均。其中一阶项因 $\sum_d(r_{id}-A_i)=\sum_d r_{id}-D A_i=0$ 而消失，二阶项里 $\frac{1}{D}\sum_d(r_{id}-A_i)^2=s_i^2$（这个肽上各工具排名的方差，即分歧度）：

$$\log G_i\approx \log A_i-\frac{s_i^2}{2A_i^2}$$

两边取指数：

$$G_i\approx A_i\left(1-\frac{s_i^2}{2A_i^2}\right)$$

也就是 **geomean ≈ mean-rank 减一个"肽内工具分歧度"的小修正**。当各肽的分歧度大致相当时，这个修正项对所有肽近似为常数、不改变肽之间的排序 → 两法排序相同 → Spearman 相同。只有在"某些肽工具吵得凶、某些肽又特别一致"且足以翻转排序时才会分开，很罕见。实测两者排序相关 0.97（median 0.90），几乎是同一个排序。

**想问老师**：能不能把"geomean 唯一"改成"geomean 是共识类融合里最稳的、有理论依据当默认，但小样本下和同类分不开"？这个"融合方法数学上是近亲"要不要写进正文当诚实点？

---

## 问题⑤ — 肽数口径对账（简短）

### 事实（陈述，不做解释）

投稿前有三个肽 / 患者口径需要统一：

| 口径 | 数值 | 来源 |
|---|---|---|
| 本地 frozen（现用管线） | **130 肽 / 9 患者** | `data/frozen/pooled_clean_9mer.csv` 已 Bash 核 |
| 袁老师侧口径 | **92 突变 / 8 患者** | 袁老师文档口径（确切定义与出处 [待核]；92 突变→肽数换算 [待核]） |
| SNV 子集 | **101 肽** | In Vitro `Variant_Type=SNV` 逐条核 |

### 想问老师

投稿正文以哪个口径为准？（这里只做事实陈述，三个口径怎么统一、差异从何而来，待您定，我方不自造解释。）

---

## 问题⑥ — 工具「呈递 vs 免疫原」分类 + headline 发现的定位

### 事实（已 Bash 核 + 文献交叉，详见 `reference/LIT_binding_vs_immunogenicity_2026-07-04.md`）
- **核心发现**：单工具预测免疫原性榜首全是 **MHC 结合预测器**（MHCnuggets 0.447 / netMHCpan_BA 0.392 / MHCflurry 0.308），打赢专门免疫原性工具（PRIME 0.294/NetTepi 0.293/…）；结合类均值 ≈ 免疫原类两倍。
- **稳健性**：剔掉边界工具后（只留无争议），结合 0.287 vs 免疫原 0.158（差 +0.129），前三名无争议 → **不是分类假象**。
- **文献一致（=数据没跑错）**：TESLA(Cell 2020)/Buckley(Brief Bioinform 2022，**同期刊**)/综述(Explor Immunol 2023)/ICERFIRE(NAR Cancer 2024) 均报「结合/呈递是强 baseline、专用工具不系统胜出、全体弱(AUC 0.5-0.65)」。
- **分类不一致点**：本地脚本用 8 呈递 vs 22 免疫原，与您 outline 的 **10 呈递 + 20 免疫原** 不完全一致；且 **MUNIS**（实为 EL 呈递模型却占免疫原名额）、**TransHLA**（归类不明）、**andy90**（内部用 binding）三个边界工具归属存疑。

### 想问老师（两点）
1. **工具分类**：投稿以您 outline 的 **10 呈递 + 20 免疫原** 权威划分为准（请确认 MUNIS/TransHLA/andy90 等边界工具各归哪类）？
2. **headline 定位**：这个「结合打赢免疫原」发现——我方建议**不写成新机制/颠覆**（已知，Buckley 2022 同期刊有先例），而是定位为「**用独立新数据(Braun 2025)+严格定量 Spearman+30 工具系统化确认**」，并 vs Buckley 划清增量（定量非 AUC/突变级非肽级/30 工具/新数据/QuantImmu 框架）。您认可这个诚实定位吗？现成 Discussion 句子 + 引用已备（见 reference 档）。

---

## 汇总：本档 6 问一览

1. **8-11mer indel 口径** — 补充分析是否限定 101 SNV 肽（路 B，推荐）/ 重跑补 indel（路 A）/ 现状加脚注。
2. **投稿 scope** — 是否 DS2-only 定稿、DS1 与鼠数据列 future work；Ex Vivo 第二读出要不要开发成正交验证轴。
3. **肽长控制** — 工具排名要不要控制肽长（原「问题一」）。
4. **geomean 近亲** — 「geomean 唯一」是否改成「共识类里最稳、小样本与同类分不开」（原「问题二」）。
5. **肽数口径对账** — 130 肽/9 患者 vs 92 突变/8 患者 vs 101 肽，以哪个为准。
6. **工具分类 + headline 定位** — 呈递/免疫原按 outline 10-20 权威划分（边界工具 MUNIS/TransHLA 归属）+ 「结合打赢免疫原」诚实定位为系统化确认（非新机制，vs Buckley 2022 划增量）。
